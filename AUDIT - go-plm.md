# АУДИТ ПРОЕКТА go-plm

**Проект:** /data/data/com.termux/files/home/go-plm
**Тип:** Pre-code specification stage — Go + React + Wails 3 Desktop PLM
**Дата:** 2026-06-07
**Аудитор:** Системный аналитик

## СВОДНАЯ ОЦЕНКА

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| **Repository Hygiene** | 5/10 | .gitignore — отлично. 12 untracked файлов, README модифицирован. Нет go.mod, нет кода. |
| **SRS Completeness** | 8/10 | 39 секций, покрытие высокое. 8 открытых вопросов, 4 нераскрытых темы. |
| **SRS Consistency** | 7/10 | 4 противоречия/расхождения, 2 дублирующихся раздела. Большинство — наследие исходников. |
| **Architecture Spec** | 8/10 | Clean Architecture описана детально. Не хватает диаграммы слоёв и примера потока данных. |
| **Domain Model** | 9/10 | 12 сущностей, все с полями и инвариантами. Стандартные детали проработаны особенно хорошо. |
| **Test Strategy** | 7/10 | Уровни тестов перечислены, примеры есть. Нет численных KPI покрытия. |
| **Roadmap Realism** | 7/10 | 13 фаз, логичная последовательность. Сроки оптимистичны для одного разработчика. |
| **Project Readiness** | 4/10 | Спецификации готовы, но разработка не начата. Нет go.mod, CMakeLists, package.json. |

**Общая оценка: 7/10** — Спецификация уровня выше среднего, готова к передаче в разработку после устранения P0/P1 замечаний.

---

## 1. REPOSITORY HYGIENE (P0-P1)

### P0 — Критические

| # | Проблема | Детали |
|---|----------|--------|
| **P0.1** | Нет go.mod | Проект заявлен как Go, но go.mod отсутствует. Невозможно `go build`. Phase 0 не выполнен. |
| **P0.2** | 12 untracked файлов | `git status` показывает 12 неотслеженных файлов. Исходные spec-файлы (7 шт) + артефакты нормализации (SRS 3 части + Complete + Epics). README.md модифицирован. Нужен коммит. |
| **P0.3** | Нет структуры директорий | `cmd/`, `internal/`, `frontend/` — не существуют. Только .md файлы в корне. |

### P1 — Важные

| # | Проблема | Детали |
|---|----------|--------|
| **P1.1** | Дублирование SRS файлов | `SRS - go-plm - Part 1-10.md`, `Part 11-14.md`, `Part 15-39.md` — промежуточные артефакты сборки. `Complete.md` — финальный документ. Части нужно удалить после верификации Complete.md. |
| **P1.2** | Исходные spec-файлы в корне | 7 черновых spec-файлов (Plm tz.md, Plm inf.md, etc.) — переместить в `docs/archive/` или `docs/source-specs/`. |
| **P1.3** | LICENSE — Apache 2.0, README — MIT | Разные лицензии. Привести к одной. |

### Позитивные моменты
- `.gitignore` — отличный, покрывает Go, Node, IDE, OS, secrets, кэш
- Git инициализирован, 2 коммита
- Все spec-файлы валидны (Markdown)

---

## 2. SRS COMPLETENESS — ПРОБЕЛЫ

### Missing Sections / Under-specified

| # | Пробел | Серьёзность | Рекомендация |
|---|--------|------------|-------------|
| **GAP-01** | Нет диаграммы потока данных (DFD) | P1 | Добавить диаграмму: Frontend → API → Commands/Queries → Store/Index → Filesystem/Git |
| **GAP-02** | Нет deployment diagram | P2 | Описать, как Wails 3 пакуется в один бинарник под Windows/macOS/Linux |
| **GAP-03** | Не описан механизм миграции index.db | P1 | SQLite schema versioning: как мигрировать при изменении схемы индекса? |
| **GAP-04** | Не описан conflict resolution для sequence | P1 | Что если два пользователя (в будущем) создают объект одного класса одновременно? Для MVP — single-user, но механизм должен быть. |
| **GAP-05** | Autosave vs Checkpoint — неясная граница | P2 | Autosave сохраняет файл. Кто создаёт commit? Когда? Нужна явная диаграмма: Edit → debounce 5s → save .md → update index. Checkpoint — отдельное явное действие. |
| **GAP-06** | Не описан формат `history.yaml.log` | P1 | Сказано "JSONL, append-only". Не показан полный формат: одна строка = один JSON? YAML-документы с `---` разделителями? В имени файла `.yaml.log`, а внутри JSONL — нужно уточнить. |
| **GAP-07** | Нет метрик качества кода | P2 | Линтеры (golangci-lint), форматтеры (gofmt), pre-commit hooks |
| **GAP-08** | Нет описания процесса сборки и релиза | P2 | Как собирается бинарник? `run.ps1` упомянут в README, но не задокументирован. Нужен Makefile/Taskfile. |

---

## 3. SRS CONSISTENCY — ПРОТИВОРЕЧИЯ И ДУБЛИ

### Противоречия

| # | Описание | Серьёзность |
|---|----------|------------|
| **CON-01** | **item.json vs только .md** — секция 8.1 говорит "objects/*/item.json" в списке Source of Truth, но секция 9.3 говорит только о .md. OQ-02 разрешён в пользу .md-only, но текст в 8.1 не обновлён. | P1 |
| **CON-02** | **Wails 2 vs Wails 3** — SRS говорит Wails 3, OQ-08 рекомендует Wails 2 из-за нестабильности v3. README.md говорит Wails 3. | P1 |
| **CON-03** | **Shop-floor в SRS** — секция 5 (Пользователи) включает «Производственный пользователь» с build records. Секция 4.2 исключает shop-floor из MVP. Секция 9.12 помечает BuildRecord как Post-MVP. Пользователь описан, но его сценарии — Post-MVP. | P2 |
| **CON-04** | **sequence для asm: 0100** — секция 9.4 (ObjectClass) говорит "0100-9999", секция 16.4 (Sequence allocation) говорит start=0100. Но naming engine должен знать, что asm начинается с 0100. Не указано, что sequence store инициализируется этими значениями при создании проекта. | P2 |

### Дублирующиеся разделы

| # | Описание |
|---|----------|
| **DUP-01** | **Security** описан дважды: секция 12.3 (NFR-SEC-01..05) и секция 26 (SEC-01..06). Разные ID требований для одного и того же. SEC-06 есть только в секции 26. Нужно объединить. |
| **DUP-02** | **Performance** описан дважды: секция 12.2 и секция 28. Идентичное содержание. Нужно оставить одно, в другом — ссылку. |

---

## 4. SRS QUALITY — СТИЛЬ И ТОЧНОСТЬ

### Позитивные стороны
- Все 39 секций структурированы логически
- Каждое требование имеет приоритет (P0-P2) и acceptance criteria
- Используется MUST/SHOULD/MAY
- Глоссарий из 25 терминов
- 46 функциональных и нефункциональных требований с ID
- YAML/Go-примеры уместны и конкретны
- Standard Parts проработан исключительно детально (duplicate levels, normalized key, classification)

### Недостатки стиля

| # | Проблема | Пример |
|---|----------|--------|
| **STY-01** | Разнобой в Markdown таблицах: где-то есть выравнивание (`|----|`), где-то нет | Секция 17.5 vs 32.2 |
| **STY-02** | `run.ps1` — PowerShell-скрипт. Не кроссплатформенно | Нужен Makefile или Taskfile |
| **STY-03** | Пути с `C:\` в примерах | `C:\cad\bracket.step` — должен быть кроссплатформенный пример |
| **STY-04** | "Нормализованный документ" в титуле — не информативно | Указать версию и дату |

---

## 5. DOMAIN MODEL — ГЛУБИНА

### Сильные стороны
- 12 сущностей с полями, инвариантами, YAML-примерами
- Relation types — полная матрица с From/To/Stored/Inverse
- Artifact Kinds с расширениями
- StandardPart — лучшая часть модели (std_class, normalized key, duplicate levels, procurement data)

### Замечания
- **Event** — не хватает списка всех EventType. Упомянуты `object.created`, `metadata.updated`, `relation.added`, `lifecycle.transitioned`, `artifact.attached`. Нет `object.deleted`, `relation.removed`, `artifact.detached`, `checkpoint.created`.
- **Project** — не указаны поля `sequences` в YAML-примере project.yaml (секция 9.2). Они есть в секции 31.1.
- **Object invariants** — п.9 "YAML frontmatter парсится без ошибок" дублирует Syntax validation уровень.

---

## 6. ROADMAP — РЕАЛИСТИЧНОСТЬ

| Фаза | Сроки SRS | Оценка реалистичности | Комментарий |
|------|-----------|----------------------|-------------|
| Phase 0 | Week 1-2 | Реалистично | Только скелет |
| Phase 1 | Week 2-4 | Реалистично | Core domain types |
| Phase 2 | Week 3-5 | Оптимистично | Параллельно с Phase 1, нужен опытный Go-разработчик |
| Phase 3 | Week 4-7 | Реалистично | Parser + naming + validation |
| Phase 4 | Week 6-8 | Реалистично | SQLite + FTS5 |
| Phase 5 | Week 7-9 | Реалистично | FSM engine |
| Phase 6 | Week 8-14 | **Оптимистично** | 6 недель на Wails + React + 8 экранов |
| Phase 7-10 | Week 12-19 | Оптимистично | BOM + StdParts + Git + Release за 7 недель |
| **MVP Total** | **Week 1-19** | **19 недель = 5 месяцев для соло-разработчика — крайне оптимистично** |

**Рекомендация**: заложить 8-10 месяцев на MVP для 1-2 разработчиков, или 5-6 месяцев для команды из 3 человек.

---

## 7. OPEN QUESTIONS — СТАТУС

| # | Вопрос | Статус | Решение |
|---|--------|--------|---------|
| OQ-01 | Глобальная библиотека std parts | OPEN | Post-MVP |
| OQ-02 | item.json vs .md | **RESOLVED** | Только .md. Но текст 8.1 не обновлён! |
| OQ-03 | CodeMirror vs Vditor | **RESOLVED** | Vditor |
| OQ-04 | Autosave vs manual save | OPEN | Autosave 5s + manual Save |
| OQ-05 | Shop-floor в MVP | **RESOLVED** | Post-MVP |
| OQ-06 | SQLite vs JSON index | **RESOLVED** | SQLite |
| OQ-07 | Bleve в MVP | **RESOLVED** | Post-MVP |
| OQ-08 | Wails 2 vs 3 | OPEN | Рекомендация: Wails 2, но README/SRS говорят Wails 3 |

---

## 8. РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТУ

### P0 — Блокирует старт разработки

1. **[P0.1]** Создать go.mod: `go mod init github.com/Homiakus/go-plm`
2. **[P0.3]** Создать структуру директорий согласно SRS 14.2
3. **[P0.2]** Закоммитить все spec-файлы
4. **[OQ-08]** Принять решение Wails 2 vs 3, обновить SRS и README

### P1 — Должно быть исправлено до начала Phase 1

5. **[CON-01]** Исправить `objects/*/item.json` в секции 8.1
6. **[DUP-01, DUP-02]** Объединить дублирующиеся разделы Security и Performance
7. **[P1.1]** Удалить промежуточные SRS Part 1-10, 11-14, 15-39 (оставить только Complete.md)
8. **[P1.2]** Переместить исходные spec-файлы в `docs/source-specs/`
9. **[P1.3]** Синхронизировать лицензию (README: MIT, LICENSE: Apache 2.0)
10. **[GAP-01]** Добавить диаграмму потока данных
11. **[GAP-03]** Описать механизм миграции index.db
12. **[GAP-06]** Уточнить формат history.yaml.log
13. **[GAP-04]** Описать конфликт sequence (для будущего multi-user)

### P2 — Улучшения (можно после старта)

14. **[CON-03]** Уточнить статус Производственного пользователя
15. **[GAP-02]** Deployment diagram
16. **[STY-01]** Унифицировать форматирование таблиц
17. **[STY-02]** Заменить run.ps1 на Makefile/Taskfile
18. **[STY-03]** Заменить `C:\` на кроссплатформенные примеры

---

## 9. ПОЗИТИВНЫЕ МОМЕНТЫ

1. **Спецификация уровня выше среднего** — 39 секций, 46 требований с ID, приоритетами и acceptance criteria
2. **Полный глоссарий** — 25 терминов, устранена путаница из исходников
3. **Standard Parts** — исключительно проработанный раздел: 11 std_classes, 5 уровней duplicate detection, normalized key с примерами для fasteners/bearings/electronics
4. **Clean Architecture** — чёткие границы слоёв, зависимости внутрь, CQS
5. **Модульная архитектура** — 22 модуля с таблицей ответственности (делает / НЕ делает / зависит от)
6. **FSM engine** — конфигурируемый через YAML, с guards и effects
7. **Transaction layer** — атомарные файловые операции с recovery
8. **Git-integration** — правильный подход: скрыть Git за инженерными действиями
9. **GitHub Epics** — 13 эпиков, 51 issue, готовых к переносу в GitHub
10. **Roadmap** — 13 фаз с зависимостями и acceptance criteria
11. **8 Open Questions** — честно задокументированы, 5 из 8 уже решены
12. **.gitignore** — отличный, покрывает все категории
